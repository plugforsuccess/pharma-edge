# Wiley Edge DXLink worker — local dev secrets template.
# Copy to .env and fill in. NEVER commit the real .env.

# Tastytrade brokerage account (production base URL — sandbox DXLink
# returns mock data, not useful here).
TASTYTRADE_USERNAME=
TASTYTRADE_PASSWORD=
TASTYTRADE_BASE_URL=https://api.tastyworks.com

# Supabase service-role key — same one the edge functions use.
SUPABASE_URL=https://rghoynbaykeyjbhqmaff.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Optional tunables (sane defaults if unset)
# FLUSH_INTERVAL_MS=750
# FLUSH_BATCH_SIZE=500
